/**
 * Phase 2–3: Auto-frame selected section (300–500ms), then route to /section/:id.
 * Section page: render master-based view, load crop, crossfade 150–250ms, remove master layer.
 */
export function useSectionTransition() {
  // TODO: startTransition(sectionId), progress, onComplete → navigate
  return {}
}
