/**
 * Three.js (or similar) scene for section 3D.
 * Load modelSrc when flip passes threshold; resize with viewport; 60fps.
 */
export function createSection3DScene(_container: HTMLElement, _modelSrc?: string) {
  // TODO: WebGL renderer, camera, load model, resize listener
  return {
    destroy: () => {},
  }
}
